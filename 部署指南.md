# 砸金蛋应用部署指南

## 目录

- [系统要求](#系统要求)
- [方式一：本地/内网部署](#方式一本地内网部署适用于活动现场)
- [方式二：云服务器部署](#方式二云服务器部署公网访问)
- [使用 Nginx 反向代理](#使用-nginx-反向代理推荐生产环境)
- [使用 PM2 保持服务运行](#使用-pm2-保持服务运行推荐)
- [安全建议](#安全建议)
- [常见问题](#常见问题)
- [部署检查清单](#部署检查清单)

---

## 系统要求

- **Node.js**: 16+ 或 18+
- **操作系统**: Windows/Linux/Mac
- **内存**: 最低 512MB
- **磁盘空间**: 最低 100MB

---

## 方式一：本地/内网部署（适用于活动现场）

此方式适合在会议、活动等现场使用，参与者通过同一局域网访问。

### 1. 安装 Node.js

- 下载地址: https://nodejs.org/
- 选择 **LTS** 版本安装
- 安装完成后验证：`node -v` 和 `npm -v`

### 2. 上传项目文件

将整个 `砸金蛋` 文件夹复制到目标服务器或电脑，例如：
- Windows: `C:\lottery`
- Linux/Mac: `/home/lottery`

### 3. 安装项目依赖

```bash
cd 砸金蛋目录
npm install
```

### 4. 启动服务

```bash
npm start
```

看到以下提示表示启动成功：
```
服务器运行在 http://localhost:3000
管理后台: http://localhost:3000/admin.html
```

### 5. 访问应用

| 访问方式 | 地址 | 说明 |
|---------|------|------|
| 本机访问 | http://localhost:3000 | 在服务器本机访问 |
| 局域网访问 | http://服务器IP:3000 | 其他设备访问 |
| 管理后台 | http://服务器IP:3000/admin.html | 配置奖品和管理 |

**默认管理员密码**: `admin123`

### 6. 大屏幕显示方案

**步骤：**
1. 使用 HDMI 线连接电脑和投影仪/大屏电视
2. 打开浏览器，访问 `http://localhost:3000`
3. 按 `F11` 进入全屏模式
4. 设置合适的缩放比例（推荐 90%-100%）

**参与者操作：**
- 确保手机和服务器在同一WiFi/局域网
- 查看服务器IP地址：
  - Windows: 打开命令行输入 `ipconfig`
  - Linux/Mac: 打开终端输入 `ifconfig` 或 `ip addr`
- 手机浏览器访问: `http://服务器IP:3000`

---

## 方式二：云服务器部署（公网访问）

此方式适合需要外网访问的场景，如线上活动。

### 推荐云服务商

- 阿里云: https://www.aliyun.com/
- 腾讯云: https://cloud.tencent.com/
- 华为云: https://www.huaweicloud.com/

### 服务器配置建议

| 配置项 | 推荐 |
|--------|------|
| CPU | 1核 |
| 内存 | 1GB |
| 带宽 | 1-3Mbps |
| 系统 | Ubuntu 20.04 / CentOS 7+ |

### 部署步骤

#### 1. 连接云服务器

**Linux/Mac:**
```bash
ssh root@你的服务器IP
```

**Windows PowerShell:**
```bash
ssh root@你的服务器IP
```

**或使用 PuTTY 工具**

#### 2. 安装 Node.js

**Ubuntu/Debian:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**CentOS:**
```bash
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

**验证安装:**
```bash
node -v
npm -v
```

#### 3. 上传项目文件

**方式1: 使用 scp**
```bash
# 在本地电脑执行
scp -r 砸金蛋 root@服务器IP:/root/
```

**方式2: 使用 FTP 工具**
- 推荐 FileZilla、WinSCP
- 主机: 服务器IP
- 用户名: root
- 密码: 服务器密码
- 端口: 22

**方式3: 使用 Git**
```bash
# 如果代码在 Git 仓库
git clone 你的仓库地址
```

#### 4. 安装依赖并启动

```bash
cd /root/砸金蛋
npm install
npm start
```

#### 5. 开放安全组端口

**重要**: 在云服务器控制台配置安全组，开放 TCP 端口 **3000**

| 云服务商 | 安全组位置 |
|----------|-----------|
| 阿里云 | ECS实例 -> 安全组 -> 配置规则 |
| 腾讯云 | 云服务器 -> 安全组 -> 修改规则 |
| 华为云 | 安全组 -> 入方向规则 |

添加规则：
- 协议: TCP
- 端口: 3000
| 源地址: 0.0.0.0/0

#### 6. 配置域名（可选）

**步骤：**
1. 购买域名（阿里云、腾讯云等）
2. 在域名控制台添加 A 记录，指向服务器IP
3. 等待DNS生效（通常10分钟内）

**访问方式：**
- 使用IP: http://你的服务器IP:3000
- 使用域名: http://你的域名:3000

---

## 使用 Nginx 反向代理（推荐生产环境）

使用 Nginx 可以：
- 使用 80/443 标准端口
- 配置 HTTPS
- 配置域名访问
- 提供静态文件缓存

### 安装 Nginx

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install nginx
```

**CentOS:**
```bash
sudo yum install nginx
```

### 创建配置文件

**创建配置文件:**
```bash
sudo nano /etc/nginx/sites-available/lottery
```

**添加以下内容：**
```nginx
server {
    listen 80;
    server_name 你的域名或服务器IP;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 启用配置

**Ubuntu/Debian:**
```bash
sudo ln -s /etc/nginx/sites-available/lottery /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

**CentOS:**
```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 配置 HTTPS（使用 Let's Encrypt 免费证书）

```bash
# 安装 certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d 你的域名

# 自动续期
sudo certbot renew --dry-run
```

配置成功后，访问 `https://你的域名` 即可。

---

## 使用 PM2 保持服务运行（推荐）

PM2 可以确保服务崩溃后自动重启，支持开机自启。

### 安装 PM2

```bash
npm install -g pm2
```

### 启动应用

```bash
cd /root/砸金蛋
pm2 start server.js --name lottery
```

### 保存配置

```bash
pm2 save
pm2 startup
```

执行 `pm2 startup` 后，会输出一个命令，复制执行它以设置开机自启。

### PM2 常用命令

| 命令 | 说明 |
|------|------|
| `pm2 list` | 查看所有进程 |
| `pm2 logs lottery` | 查看日志 |
| `pm2 logs lottery --lines 100` | 查看最近100行日志 |
| `pm2 restart lottery` | 重启应用 |
| `pm2 stop lottery` | 停止应用 |
| `pm2 delete lottery` | 删除应用 |
| `pm2 monit` | 实时监控 |

### PM2 配置文件（可选）

**创建 `ecosystem.config.js`:**
```javascript
module.exports = {
  apps: [{
    name: 'lottery',
    script: 'server.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production'
    }
  }]
};
```

**使用配置文件启动:**
```bash
pm2 start ecosystem.config.js
```

---

## 安全建议

### 1. 修改默认管理员密码

**方式1: 通过管理后台修改**
- 访问 http://你的地址/admin.html
- 使用默认密码 `admin123` 登录
- （未来版本将支持密码修改功能）

**方式2: 直接修改数据库**
```bash
cd database
sqlite3 lottery.db
UPDATE config SET value = '你的新密码' WHERE key = 'admin_password';
.exit
```

### 2. 配置防火墙

**Ubuntu (UFW):**
```bash
sudo ufw allow 3000/tcp
sudo ufw enable
sudo ufw status
```

**CentOS (firewalld):**
```bash
sudo firewall-cmd --permanent --add-port=3000/tcp
sudo firewall-cmd --reload
sudo firewall-cmd --list-ports
```

### 3. 数据库备份

**手动备份:**
```bash
cp database/lottery.db database/lottery.db.backup.$(date +%Y%m%d)
```

**自动备份脚本 (`backup.sh`):**
```bash
#!/bin/bash
BACKUP_DIR="/root/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
cp database/lottery.db $BACKUP_DIR/lottery_$DATE.db
# 保留最近30天的备份
find $BACKUP_DIR -name "lottery_*.db" -mtime +30 -delete
```

**设置定时任务:**
```bash
crontab -e
# 每天凌晨2点备份
0 2 * * * /root/backup.sh
```

### 4. 恢复数据库

```bash
# 停止服务
pm2 stop lottery

# 恢复数据库
cp backup/lottery_20240101.db database/lottery.db

# 重启服务
pm2 start lottery
```

---

## 常见问题

### Q1: 端口被占用怎么办？

**方法1: 修改端口**
编辑 `server.js`:
```javascript
const PORT = 3000;  // 改为其他端口，如 8080
```

**方法2: 查找并停止占用进程**
```bash
# 查找占用进程
netstat -ano | findstr :3000  # Windows
lsof -i :3000                 # Linux/Mac

# 停止进程
taskkill /F /PID 进程号        # Windows
kill -9 进程号                 # Linux/Mac
```

### Q2: 如何更新应用代码？

```bash
# 停止服务
pm2 stop lottery

# 上传新代码（替换旧文件）

# 如果有新依赖
npm install

# 重启服务
pm2 restart lottery
```

### Q3: 数据库文件在哪里？

- 位置: `database/lottery.db`
- 包含内容:
  - 奖品配置
  - 抽奖记录
  - 管理员密码
  - 金蛋配置
  - 音效配置

### Q4: 如何清空所有数据重新开始？

```bash
# 停止服务
pm2 stop lottery

# 删除数据库
rm database/lottery.db

# 重启服务（会自动创建新数据库）
pm2 start lottery
```

### Q5: 上传的图片保存在哪里？

- 位置: `public/uploads/`
- 注意: 更新代码时请保留此目录

### Q6: 如何查看服务器日志？

```bash
# PM2 日志
pm2 logs lottery

# 实时查看
pm2 logs lottery --lines 100 -f
```

### Q7: 浏览器无法访问怎么办？

**检查清单:**
1. 服务是否启动: `pm2 list`
2. 端口是否开放: 检查防火墙/安全组
3. 服务器IP是否正确
4. 是否在同一网络（内网部署）
5. 查看日志: `pm2 logs lottery`

### Q8: 金蛋图片不显示？

**可能原因:**
1. 图片路径错误
2. 图片文件不存在
3. 文件权限问题

**解决方法:**
```bash
# 检查文件权限
chmod 644 public/uploads/*
chmod 755 public/uploads
```

---

## 部署检查清单

部署完成后，请确认以下项目：

### 基础环境
- [ ] Node.js 已安装（版本 16+）
- [ ] npm 可用

### 项目部署
- [ ] 项目文件已上传到服务器
- [ ] 依赖已安装 (`npm install`)
- [ ] 服务已启动 (`npm start` 或 `pm2 start`)

### 网络配置
- [ ] 防火墙已开放端口 3000
- [ ] 云服务器安全组已开放端口 3000
- [ ] 服务器IP可访问
- [ ] （可选）域名已解析

### 功能测试
- [ ] 浏览器可访问 http://服务器IP:3000
- [ ] 金蛋页面显示正常
- [ ] 管理后台可访问 http://服务器IP:3000/admin.html
- [ ] 可以登录管理后台
- [ ] 已修改默认管理员密码
- [ ] 奖品配置完成
- [ ] 库存设置正确
- [ ] 抽奖功能测试正常
- [ ] 音效播放正常

### 生产环境（如适用）
- [ ] 使用 PM2 管理进程
- [ ] 配置开机自启
- [ ] 配置 Nginx 反向代理
- [ ] 配置 HTTPS 证书
- [ ] 设置数据库定期备份
- [ ] 配置日志轮转

### 现场活动（如适用）
- [ ] 大屏幕显示正常
- [ ] 局域网访问正常
- [ ] 参与者手机可访问
- [ ] 全屏模式显示正常
- [ ] 已清空旧数据（如需要）

---

## 技术支持

如遇到问题，请检查：
1. PM2 日志: `pm2 logs lottery`
2. Nginx 日志: `sudo tail -f /var/log/nginx/error.log`
3. 系统日志: `sudo journalctl -xe`

---

**祝活动顺利！** 🎉
